# .github/workflows/hmot-keeper.yaml
name: HMOT Keeper

on:
  schedule:
    - cron: "*/5 * * * *"
    - cron: "2-59/5 * * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: hmot-sweep
  cancel-in-progress: false

jobs:
  sweep:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      # ---- required ----
      RPC_URL: ${{ secrets.RPC_URL }}
      PRIVATE_KEY: ${{ secrets.BOT_PRIVATE_KEY }}
      CONTRACT_ADDRESS: ${{ secrets.GAME_ADDRESS }}
      CHAIN_ID: ${{ vars.CHAIN_ID }}              # e.g., 84532 for Base Sepolia

      # ---- moderation providers (optional/fallbacks) ----
      PERSPECTIVE_API_KEY: ${{ secrets.PERSPECTIVE_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      # optional throttle (ms) between txs
      RATE_LIMIT_MS: ${{ vars.RATE_LIMIT_MS }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm i
          fi

      - name: Build
        run: npm run build --if-present

      - name: Run keeper
        run: |
          if [ -f dist/keeper.js ]; then
            node dist/keeper.js
          else
            npx tsx src/keeper.ts
          fi
